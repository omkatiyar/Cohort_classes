// local storage stores the token. token is stored in local storage inside the application tab of developer tools window.
// authorization key is present inside headers
// the token is stored in the browser using: localStorage.set("token")